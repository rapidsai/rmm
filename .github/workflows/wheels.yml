name: RMM wheels

on:
  push:
    branches:
      - 'pull-request/[0-9]+'

concurrency:
  group: "${{ github.workflow }}-${{ github.ref }}"
  cancel-in-progress: true

jobs:
  rmm-wheels:
    uses: rapidsai/shared-action-workflows/.github/workflows/wheels-manylinux.yml@feat/wheel-ci-actions-2
    with:
      package-dir: python
      package-name: "rmm"
      python-package-versioneer-override: "22.10.00a"
      python-version: "3.8 3.9"
      python-package-build-tag: "5"

      test-extras: "test"
      test-unittest: "python -m py.test -v ./python/rmm/tests"
    secrets: inherit
