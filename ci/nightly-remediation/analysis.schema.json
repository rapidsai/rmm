{
  "type": "object",
  "additionalProperties": false,
  "required": ["failures"],
  "properties": {
    "failures": {
      "type": "array",
      "maxItems": 32,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["signature", "title", "root_cause", "evidence", "job_ids", "kind", "runner", "validation_plan"],
        "properties": {
          "signature": {"type": "string", "minLength": 1, "maxLength": 200},
          "title": {"type": "string", "minLength": 1, "maxLength": 200},
          "root_cause": {"type": "string", "minLength": 1, "maxLength": 4000},
          "evidence": {"type": "string", "minLength": 1, "maxLength": 6000},
          "job_ids": {"type": "array", "minItems": 1, "uniqueItems": true, "items": {"type": "integer", "minimum": 1}},
          "kind": {"enum": ["build", "dependency", "test", "infrastructure", "unknown"]},
          "runner": {"enum": ["cpu", "gpu"]},
          "validation_plan": {"type": "string", "minLength": 1, "maxLength": 4000}
        }
      }
    }
  }
}
